/**
 * D5 — Patient detail (view) + enrolment (create).
 *
 * The router pins these two named exports; the implementations live in
 * `detail-page.tsx` and `create-page.tsx`.
 */
export { PatientDetailPage } from './detail-page';
export { PatientCreatePage } from './create-page';
